{
  "name": "@subspace/logger",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "pino": "^10.1.0",
    "pino-std-serializers": "^7.0.0"
  }
}
