describe.skip("SendgridTransport integration", () => {
  it("sends email in sandbox mode without delivery")
  it("returns valid messageId in sandbox mode")
  it("rejects with invalid API key")
  it("sends with scheduled sendAt without error")
  it("sends with tracking options without error")
})
